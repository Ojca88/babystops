# Copy to .env.local and fill in from your Supabase project settings
# (Project Settings -> API), or run `vercel env pull .env.local` once
# the project is linked and the Supabase integration is provisioned.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
